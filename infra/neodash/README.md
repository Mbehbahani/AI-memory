# NeoDash

`dashboard.json` is created by A11 in P12-T01 (pages: Project Map, Decisions Timeline, Documents &
Sources, Tasks/Experiments, Provenance drill-down). Loaded by the `neodash` service (profile `viz`) at
`http://127.0.0.1:5005`, connecting with the read-only Neo4j user.
