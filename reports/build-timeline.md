# Build Timeline

| When (local) | Phase / Task | Agent | Event |
|---|---|---|---|
| 2026-09-13 15:45–16:05 | P0-T01 | A00 | Read-only environment and source audit for the planning report |
| 2026-09-13 ~16:10 | — | owner | Planning report delivered; decisions AC-1…AC-10 pending owner review |
| 2026-09-13 16:19 → | P2-T01 | A00 | Repository scaffold created (no services built or started) |
| — | P2-T03 | A00 | Waiting for START DEVELOPMENT: git init + first commit, then P1 → P17 |
| 2026-09-14 | P2-T05 | A00 | Owner review: AC table, frugal-RAM defaults, evaluation protocol (ADR-0010) added |
| 2026-09-14 | — | owner | **START DEVELOPMENT** given; orchestrator begins P0 → P17 |
| 2026-09-14 (exact time not available) | P0-T01 (re-run) | A00 | Environment re-audit at start of development: Docker daemon reachable (server 29.7.2, Compose v5.4.0, 8 CPU / 15.46 GiB VM, 0 containers/images), all 8 ports free, both source roots readable. Verdict PASS; no blockers. Wrote `reports/environment-audit.md` |
| 2026-09-14T08:26Z → 2026-09-14T09:14:31Z | P1-T01 | A02 (Opus) | Domain models, ports, ontology loader, config built and synced with `schemas/`; 19 files created, 7 modified. `pytest tests/unit -q` → 157 passed, 1 skipped in 0.60s; ruff and mypy clean. Blocker noted: host Python 3.14/3.13 incompatible with pyproject's `>=3.12,<3.13` pin, worked around with a gitignored `.venv-contracts` (Python 3.13.14) — needs container-side re-verification once the tools image exists (P2-T02) |
| 2026-09-14T08:26Z → 2026-09-14T09:14:31Z | P1-T02 | A02 (Opus) | Architecture docs written: `docs/architecture/{data-model,ontology,temporal,retrieval}.md`, each documenting deviations from `v0.1-plan.md` (10/4/5/7 respectively). Acceptance verification ("review by A00") not yet performed — open item |

## Reconciliation note (A01, 2026-09-15)

The build was interrupted (unrecorded power loss) after the row above was last written. HEAD at
reconciliation is `2c87f86`. The entries below are **reconstructed from `git log --stat`, commit
messages, and the reports the agents wrote themselves**, not from live task-result observation. Exact
task-level start/end clock times were never captured for this period; only commit dates are known
(recorded as **commit date**, which is when work was saved, not necessarily when it started or finished).
A recurring pattern across this period: background agents were **repeatedly terminated mid-task by
session/API rate limits**; A00 (orchestrator) performed direct verification in several cases
(P3, part of P4) rather than trust an unfinished agent's own claims, and later commits recovered and
completed partial work from earlier ones (P6-T03, P8). This is stated explicitly in `reports/infra-bringup.md`'s
provenance note and in several commit messages, and is preserved here rather than smoothed over.

| When (local / commit date) | Phase / Task | Agent | Event |
|---|---|---|---|
| commit `01337bfdcdf776c92c3f39baa9d2d3404cd1fe57`, 2026-09-14 10:39 +02:00 | P2-T03 | A00 | First commit ("Master plan") — repository scaffold, P0-T01 re-audit, and the P1-T01/T02 work all land in this single commit. (Corrects the earlier task-ledger note that P1-T01/T02 were "uncommitted" — they were committed here.) |
| commit `0e65d6d`, 2026-09-14 11:48 +02:00 | P1+P2 | A02, A03, A00 | "P1+P2: architecture contracts, Dockerfiles, locks, scripts." P2-T02 (A03): 4 service Dockerfiles + tools image, two pinned lock files on Python 3.12, image digests recorded, 9 operator scripts; commit message states all five images build. |
| commit `f3293f0`, 2026-09-14 15:14 +02:00 | P3 | A03/A06 (code), A00 (verification) | "P3: core infrastructure up and verified." Postgres+Neo4j healthy (extensions + 190 APOC procs); qwen3:4b pulled (2.5 GB, digest `359d7dd4bcda`), schema-constrained decoding smoke-tested (9.2 tok/s gen, 32.7 tok/s prompt on a 28-token sample); embedding-service verified offline (`--network none`), 384-d, deterministic, 30.7 texts/s. **All four background agents (A03, A06, and two others) were terminated mid-run by a session limit; A00 performed and recorded every verification directly** rather than inheriting the agents' unfinished claims — see `reports/infra-bringup.md`'s provenance note. |
| commit `606d8a3`, 2026-09-14 20:24 +02:00 | P5 (complete), P4/P6 (partial) | A04, A05, A07b | "P5 complete; P4/P6 partial (agents cut off by rate limit)." P5-T01/T02 (A04): Alembic 0001+0002 (28 tables + `provenance_v`), `uq_facts_functional_current` enforcing ADR-0005 rule 1, `Neo4jGraphStore`; **finding**: Neo4j Community cannot enforce a read-only user (role-less user ran `CREATE`; `GRANT ROLE` unsupported) — flagged for an ADR. P4 (A05, partial): `OllamaProvider` + validation written; measurements and Graphiti gate not yet run. P6-T01/T02 (A07b, partial): extractors, chunkers, policies/ignore/secrets, adversarial fixtures written, **not yet verified**. **Test state: 342 passed, 1 failed, 1 skipped** — failure is a real-vault chunking bounds test (264-token chunk vs 250-token assertion on a LinkedIn tracking-URL blob); left failing rather than loosening another agent's assertion for a false-green board. |
| commit `96b751a`, 2026-09-14 20:37 +02:00 | ADR-0012 | A00 / owner | "ADR-0012: dual extraction providers (local qwen3:4b + Bedrock Haiku 4.5)." Owner decision: build and compare both, neither privileged as reference. `BedrockProvider` added behind the frozen `LLMProvider` port using forced tool use for structured output. n=1 smoke comparison: qwen3:4b 65.97s vs Haiku 4.5 6.59s, both schema-valid first attempt; qwen3 mistyped `JobLab Lakehouse` as `Dataset` (should be `Project`). Amends B16/plan §T/AC-8: Bedrock calls now leave the machine. Test state unchanged from the previous commit (342 passed, 1 failed, 1 skipped). |
| commit `8e495ca`, 2026-09-14 21:24 +02:00 | P5-R01, ADR-0013, P6-T02 fix | A00 (review), A04 (finding), A07b (fix carried) | "ADR-0013 + P5-R01 review." P5-R01: A00 (Opus) independently verified `uq_facts_functional_current`, `ck_facts_window`, and the 25-value predicate CHECK directly against the live database — **APPROVED**. ADR-0013 formalizes A04's P5-T02 finding: Neo4j Community Edition cannot enforce role-based read-only access (`GRANT ROLE`/`SHOW ROLES` → "Unsupported administration command"); replaced with three real layers (client-side write refusal, ADR-0001 rebuildability, ADR-0007 loopback-only) and the absence stated plainly for A13's P15 threat model. Also in this commit: the P6-T02 chunking bounds bug from `606d8a3` is fixed — hard-split slice size is now recomputed from the buffer's *remaining* token budget before every cut, instead of the full budget once, bounding overshoot to rounding error. |
| commit `1562ba9`, 2026-09-15 01:29 +02:00 | P4-T02 (data), P4-T04 (attempt), P6-T03 / P2-T04 (partial) | A05, A07a, A12 | "P4-T02 complete (both providers, n=20); P6-T03 and P2-T04 partial." A05 completed the full n=20 measurement campaign for **both** providers before being cut off by a rate limit — died before writing the report; raw results committed (`tests/evaluation/local_ai/results/bench-{ollama,bedrock}-p4t02.json`). Graphiti gate attempted (Bedrock branch only): crashed on episode E01 — `TypeError: AsyncMessages.create() got an unexpected keyword argument 'temperature'` inside `graphiti_core/llm_client/anthropic_client.py`; no Ollama gate run exists; ADR-0009 still pending. A07a (ingestion core, P6-T03) and A12 (test scaffolding, P2-T04) both cut off mid-work; partial files committed so nothing was lost. |
| commit `a193e18`, 2026-09-15 01:31 +02:00 | P4-T02 (write-up), P4-T04 (write-up) | A00 | "P4-T02 analysis + Graphiti gate report." A00 wrote up the completed P4-T02 campaign and the failed Graphiti gate directly from the committed raw result files. **Key MEASURED findings**: first-pass JSON validity tied at 95% for both providers; entity recall tied at 68.9%; relationship recall qwen3:4b 40.2% vs Haiku 61.2% (plan C3 requires ≥50% — qwen3:4b **fails** the plan's own bar); median s/episode 174.43 (ollama) vs 7.74 (bedrock) = 22.5×; Ollama RSS with model resident median 8.01 GiB / p90 10.82 / **max 11.37 GiB** (retroactively validates declining AC-9 — a 12 GB WSL2 cap would have OOM-killed extraction); Bedrock cost MEASURED $0.0083/episode, ~$1.66 extrapolated for the vault (corrects ADR-0012's pre-campaign "<$1" estimate). Graphiti gate: no clean verdict from the crashed run, but the Ollama branch is settled by independent arithmetic (6–10 calls × 174.43s median = 1047–1744s vs C2's 240s threshold, FAIL 4.4–7.3×). Also recorded: Graphiti left 24 indexes + 18 constraints in the live Neo4j, cleanup never ran — A08/A11 must diff against `constraints.cypher` before dropping anything. |
| commit `8d54ebc`, 2026-09-15 02:10 +02:00 | ADR-0009, ADR-0014 | A00 (Opus) / owner | "ADR-0009 + ADR-0014: native engine; Haiku default; one model per corpus." Owner instruction: "select the best approach and prevent mismatch … use the best approach." **ADR-0009**: native temporal engine, not Graphiti — decided on independent P4-T02 arithmetic (the crashed gate is explicitly *not* cited as the reason); native engine is provider-agnostic (works with both LLM providers) and needs only 2–3 calls/episode vs Graphiti's 6–10. **ADR-0014**: Bedrock Haiku 4.5 becomes the default extraction provider (discriminator: relationship recall 61.2% vs 40.2%, plan requires ≥50%); mismatch prevention made structural — one extraction model per corpus (pipeline hard-refuses to extract into a project whose current facts came from a different `extraction_model_id`; remedy is explicit re-extraction via ADR-0005 supersession; `--allow-model-mix` exists for benchmarking only); deterministic seeding (PARA folders, registry projects) wins where both models are systematically wrong; `secret_suspected` sources have no stored text so nothing can be transmitted. **Cost stated plainly**: B16 no longer holds by default; vault content including CVs and job applications goes to AWS during extraction; AC-8 holds only under `LLM_PROVIDER=ollama`. |
| commit `2c87f86`, 2026-09-15 09:38 +02:00 | P6-T04, P7 (partial), P8-T01/T02/T03 | A07a, A08, A12 | "P6-T04/P7/P8: recover in-flight work; memory scenario suite green." Recovered uncommitted work that was in flight when the machine powered off: native engine, temporal rules/store/artifacts, entity resolution, provenance builders/explain, structural graph + Neo4j projection writer, Tier 0 seeds, a Tier 2 run, the P6-T04 scenario suite and its support module. Fixed the two scenarios still failing (see `reports/test-summary.md` for the verbatim diagnosis of each). **Verified in the tools image: 428 passed, 1 skipped.** Surfaced and explicitly documented (not silently fixed) a known limitation: `--allow-model-mix` re-confirmation via `FactRepo.touch` does not restamp `extraction_model_id`, so `extraction_models_in_use` under-reports how many models actually touched a corpus — closing it is an ADR decision for A04/A08. **Not claimed as complete**: P7-T01/T02/T03 (Tier 0 registry seed, Tier 1 vault embedding, structural graph projection) and P8-T04 (Tier 2 run on my-vault) have supporting code on disk but **no evidence of an actual run against `D:\My-Vault` or `joblab-de`** was found — no `aimemory-ingest status` coverage output, no live Cypher counts. These remain `todo` in `docs/development/task-plan.md` pending a real run and its measured output. |
| 2026-09-15 (measured by the orchestrator) | full suite | orchestrator | `docker compose --profile tools run --rm tools pytest -q` → **428 passed, 1 skipped**. The skip is `tests/unit/test_contracts_uri.py:206` (Windows-specific junction reparse points, not a failure). This is the authoritative current test state; see `reports/test-summary.md`. |
| 2026-09-15 | — | A01 | Reports (`agent-runs.jsonl`, `task-ledger.md`, `build-timeline.md`, `test-summary.md`, `CHANGELOG.md`) reconciled against the evidence above after the recorded interruption. `docs/development/task-plan.md` status column updated for tasks demonstrably complete at HEAD; P7-T01/T02/T03 and P8-T04 left `todo` — code exists but completion is not evidenced. |

## Reconciliation note (A01, 2026-09-17 — nine commits since the last pass)

HEAD at the previous reconciliation was `2c87f86`. HEAD is now `c462170`. `git log 2c87f86..HEAD`
contains **nine** commits — one more than the eight the orchestrator listed when dispatching this
pass. The extra one, `755b06f`, is a child of `2c87f86` and is itself the previous A01 reconciliation
commit (it wrote every report this file continues from); its own A03 code change — wiring AWS/Bedrock
credentials into the containers — was never given a timeline entry, so it is included below. Most of
the remaining eight commits carry unusually detailed, accurate commit messages with their own MEASURED
sections; A01 quotes them directly and flags where A01 has not independently re-run a test.

| When (commit date) | Phase / Task | Agent | Event |
|---|---|---|---|
| commit `755b06f`, 2026-09-15 09:55:42 +02:00 | P3-T01 (Bedrock credentials fix) | A03 | "Bedrock credentials in containers; reconcile build reports after power loss." `LLM_PROVIDER=bedrock` is the ADR-0012/ADR-0014 default, but no compose service passed AWS credentials or region, and neither the ingestion nor the tools image installed the `bedrock` extra — boto3 was absent entirely, so the default provider could not authenticate from any container. Adds an optional read-only `~/.aws` bind mount (`HOST_AWS_DIR`, defaulting to a checked-in empty dir so the mount is genuinely optional), `AWS_PROFILE=mohabehb` and `us-east-1` per the account registry, and the `bedrock` extra to both images. `requirements.lock` regenerated (boto3 + 6 transitive deps, no existing pin changed). No credential stored in the repo. Per the commit message (not independently re-verified by A01): credentials resolve inside both images against the real provider code path; with AWS config stripped, the mount falls back to the empty dir and fails cleanly with `ProfileNotFound` rather than silently picking up stray host state. This same commit is the previous A01 reconciliation pass (wrote `reports/*.md` and `CHANGELOG.md` through `2c87f86`) — flagged a known blocker it did not fix: `apps/ingestion/Dockerfile` never `COPY`s `infra/postgres/alembic`, so `docker compose up -d` could not get past migrate. Assigned to A03; fixed next commit. |
| commit `3b80de9`, 2026-09-15 10:02:44 +02:00 | P3-T01 (image build-context fix) | A03 | "Copy alembic + neo4j schema into the image so migrate can run." Fixes exactly the blocker `755b06f` flagged: `cli/migrate.py` resolves `_REPO_ROOT` as `parents[3]` (`/app` in the image) and reads `infra/postgres/alembic/alembic.ini` and `infra/neo4j/schema/*.cypher` relative to it, but neither tree was ever `COPY`ed in. `docker compose up -d` had died at migrate with `"No 'script_location' key found in configuration"` for every build since the image was first written. Both trees copied in one edit — the Postgres and Neo4j gaps are the same bug one step later (`migrate()` applies Postgres migrations then Neo4j constraints), so fixing only alembic would have failed again immediately. `migrate.py` itself untouched — its path logic was already correct for the container layout; only the image contents were wrong. Verified (commit message): migrate exits 0; `alembic_version = 0002_ops_eval`; 29 tables; 18 Neo4j uniqueness constraints; `memory_reader` user present; a second migrate run is a clean no-op; all ports still bound to `127.0.0.1` only. `memory-api` and `mcp-server` still did not start at this commit (`ModuleNotFoundError: no apps.memory_api`) — expected, fixed in `43d8a2b`. |
| commit `ef30037`, 2026-09-15 10:02:57 +02:00 | P9-T01 | A09 | "Vector + keyword candidates, RRF fusion, boosts." Hybrid retrieval stops at `RankedCandidate`; conversion to `ScoredHit` (needs resolved `Provenance`, stage 7/P10) happens in exactly one place, so no frozen contract was touched. Deterministic ordering at every stage (within a retriever, in fusion, after boosting) so two identical queries return byte-identical rankings. Query vector bound as a text literal and CAST in SQL rather than via an adapter — `pgvector.psycopg` registers dumpers for `Vector` and `numpy.ndarray` only, so a bare `list[float]` reaches the server as `float8[]`, accepted on INSERT but not as an operand of `<=>`. Deliberate, documented deviation: `since` is evaluated on `source_versions.observed_at` (observation axis), not `chunks.created_at` — `docs/architecture/retrieval.md` §1 used `created_at` (ingestion time), a different question than §5 and `temporal.md` §8/deviation 5 both answer; code follows the observation axis, handed to A02 to fix the doc (done in `ff19689`). MEASURED (commit message, not independently re-verified by A01): 518 passed, 2 skipped (+90 tests); mypy clean on all 8 retrieval modules; end-to-end `retrieve()` over a 16-chunk fixture corpus with real MiniLM vectors, median 16.8–17.2 ms across 3 queries × 5 samples. Retrieval quality (recall/precision) explicitly UNKNOWN at this commit — the dev database held 0 chunks/0 embeddings, no Tier-1 corpus existed yet; a smoke test against a real corpus was written but skipped honestly until `aimemory-ingest` had run. |
| commit `ff19689`, 2026-09-15 10:08:00 +02:00 | P9-T01 (doc correction) | A02 | "Correct retrieval.md against the shipped P9-T01 code." Verdict on the drift A09 reported: the code is right, the doc was wrong. §1's `since` filter used `chunks.created_at` against three concurring statements (§5, `temporal.md` §8 and its deviation 5) that put `since` on the observation axis; no ADR ever placed `since` on any axis (`grep -rn "since" docs/adr/*.md` is empty), so this corrects a drafting slip rather than reversing a decision — the prior wording is preserved as deviation 8. §2 was worse than drift: the published keyword-search SQL was invalid (`FROM chunks c, plainto_tsquery(...) q JOIN sources s ON s.id = c.source_id` binds the JOIN to the tsquery relation, putting `c` out of scope in its `ON` clause) — confirmed against the live database: `"invalid reference to FROM-clause entry for table c"`. Rewritten to the shipped shape. Also documents the real boost keys, `boosts.low_trust_penalty`, the vector-literal cast, and which pipeline stages are actually built (0–3 and 6 shipped; 4, the fact side of 5, and 7–8 still spec) so unbuilt sections are no longer mistakable for implemented ones. |
| commit `43d8a2b`, 2026-09-17 06:47:42 +02:00 | P10-T01, P10-T02 | A09 | "P10: graph expansion, temporal filter, provenance, context, Gateway, memory-api." **memory-api reaches healthy for the first time.** The restart loop was a placeholder `CMD uvicorn apps.memory_api.main:app` that could never import — the directory is `apps/memory-api` and a hyphen cannot appear in a Python module path; fixed the way `embedding-service` already does it. Loopback decision recorded: uvicorn binds `0.0.0.0` *inside* the container; the guarantee is the published port (`127.0.0.1:8000`) per ADR-0007 and the `embedding-service` precedent — binding the container's own `127.0.0.1` would make the service unreachable from `docker-proxy` and `mcp-server` on the compose network; now mechanically enforced by a test that parses both compose files and fails on any non-loopback publish. Degradation proved by simulated outage, not docstring: a graph double whose every call raises `ConnectionError` still returns cited hits with complete provenance, invents no `entity_linked` boost, and records the warning in both the response and the stored `retrieval_logs` row. `/health` returns 200 whenever Postgres is reachable; Neo4j or the embedder down is "degraded", not "down" — both have defined degraded modes, and failing the healthcheck for a survivable condition would keep `mcp-server` from ever starting. `as_of` reads now filter on the validity window only — an earlier `current_status <> 'superseded'` clause was removed because it hid artifacts genuinely current at an earlier `as_of`, defeating a point-in-time read. MEASURED (commit message, not independently re-verified by A01): 107 passed in the retrieval/gateway subset (run without the destructive migration test); mypy clean on `apps/memory-api` and owned packages; OpenAPI export 14 paths, `--check` enforced. |
| commit `37b90da`, 2026-09-17 06:47:57 +02:00 | P7-T01, P7-T02 | A07a | "P7-T01/T02: first real vault ingest; six ingestion bugs fixed." **MEASURED on `D:\My-Vault` (read-only, `RW=false` confirmed)**: 243 files on disk, 63 inside pruned dirs, 4 ignored, 176 registered. 143 INDEX_CONTENT, 2 MIRROR, 31 CATALOG_ONLY. 2,838 chunks, 2,779 embeddings, 27 projects, 64 aliases, 35 deterministic entities. 2m33s, reproducible across three runs with identical counters. Zero LLM calls and zero cloud calls — verified, not assumed. P7-T01 acceptance met exactly: 27 projects = 7 from `me.md` + 21 from `project-graph.md` − 1 merged on slug. P7-T02 is **98.6% (143/145), not 100%**, reported that way deliberately: one 192-byte frontmatter-only file correctly yields 0 chunks; one file is byte-identical to another and its text is embedded under its twin — the denominator was not adjusted to reach 100%. Six ingestion bugs fixed, all in owned code: (1) `--tier 0` then `--tier 1` produced nothing — tier applied only to *changed* versions, so the second scan saw every file as unchanged; tiers are now a ceiling *and* a debt (also the resume path); (2) the CATALOG_ONLY downgrade on extraction failure was rolled back by the very exception that triggered it, leaving six files INDEX_CONTENT with no text and no reason recorded; (3) registry parsing matched `business` inside "Research track (keep separate from business content)" and filed 3 research projects under business; (4) path commentary split on commas into junk aliases that mis-filed documents; (5) stale `running` run rows were never closed after a killed scan. Restartability proved, not asserted: killed mid-scan and resumed — of 29 pre-kill versions, 0 changed their chunk count; 2,834 chunks all distinct; 0 duplicate `(version_id, ordinal)`; a further scan writes nothing in 5.8s. |
| commit `670f516`, 2026-09-17 11:00:29 +02:00 | P2-T04 (corpus-safety fix) | A12 | "A12: stop the test suite from destroying the ingested corpus." `test_migrations.py` ran `alembic downgrade base` against the shared dev database — the file said so ("Destructive by design") and CLAUDE.md carves out an exception for exactly that command, so the test was never wrong, but once a real corpus existed a plain `pytest -q` dropped it. **Two agents lost the vault corpus to this independently**; one captured the evidence — the `sources` table's OID changing across a run while `pg_stat_user_tables.n_tup_del` stayed 0, i.e. a DROP + CREATE, not a DELETE. Fixed: the whole module now runs against a throwaway database created per module and dropped at teardown, so the P5-T01 acceptance still runs by default rather than being deselected. A second bug surfaced while verifying: the scratch fixture yielded `str(url)`, and SQLAlchemy's `URL.__str__()` masks the password as `***`, so every test using that DSN failed authentication — fixed by rendering with `hide_password=False`. Also fixed `test_claim_next_and_mark_failed`, which assumed `EpisodeRepo.claim_next()` would return the episode it created — the claim is global over the queue, so real pending vault episodes outranked the fixture and it failed against real data while passing in isolation, exactly backwards; the test now moves competing episodes aside inside its own transaction, which `pg_session` rolls back, leaving production behaviour untouched. **MEASURED (commit message): full suite 627 passed, 1 skipped.** Corpus counts identical before and after that run — 176 sources / 2,838 chunks / 2,779 embeddings / 27 projects / 144 pending episodes. |
| commit `78561f3`, 2026-09-17 11:01:01 +02:00 | P6-T02 (chunker fix) | A07b | "A07b: bound the two chunker shapes that escaped the token budget." Both defects MEASURED during the first real vault ingest (`37b90da`); regression fixtures are synthetic since the vault is a read-only source root and never a fixture source. `markdown.py`: a fence-open transition appended to the buffer without flushing it, so prose sitting just under `max_tokens` absorbed an entire fenced block before any size check could fire — a real note produced a single 28,446-character (~8,366-token) chunk against a 200-token target. Fence open/close are now hard boundaries like an ATX heading, unconditional and deliberately not size-gated (a size gate at the fence boundary is no gate at all). `code.py`: the line window could never shrink below one whole physical line, so a one-line 12,623-character `.json` file became a single chunk, rejected by the embedding service with `422 texts[0] exceeds 8000 characters`; a single line over budget is now split within the line — the one place this chunker cuts inside a line, preferring the last whitespace inside the budget and cutting mid-token only when there is none (the minified-JSON case). Both module rules still hold and are tested: a fenced block is never split, a `#` inside a fence is never a heading. MEASURED after the fix: the one-line JSON shape goes from 1 chunk to 27 (max 200 tokens/680 chars); the fenced-note shape no longer merges prose into the block. **Remaining limitation, by design**: a *standalone* fence larger than the budget is still one oversized chunk (rule 1) — measured at 10,191 chars, still above the embedder's 8,000-char limit, so the pipeline's hard-split backstop still carries it; whether rule 1 should yield to the embed limit is an open design question for A07b/A02. |
| commit `c462170`, 2026-09-17 11:19:45 +02:00 | P8-T02 (wiring fix) | A08 | "P8-T02 wiring: expose the knowledge engine and writer so Tier 2 can actually run." `aimemory-ingest tier2` reported "no KnowledgeEngine available (not built yet) — queue left untouched" although the engine was already built and committed: `packages/aimemory/knowledge/__init__.py` was a bare docstring, so `from ..knowledge import get_engine` raised `ImportError`, swallowed by a broad `except` in `get_knowledge_engine` into a misleading "not built yet" message. Adds `get_engine` (native engine per ADR-0009, building its own provider from `LLM_PROVIDER`) and `get_writer` (over `make_result_writer`); imports kept lazy inside the functions since constructing the engine reaches `providers.llm`, which reaches back into config — a module-level import would cycle, and importing the package now never requires credentials or a database. Second gap found while fixing the first: `run_tier2` persisted only `if writer is not None`, and every CLI path passed `None` — an engine with no writer would have called the LLM for every episode and discarded every result, paying for the extraction and the egress to produce nothing; `run_tier2` now defaults the writer and refuses to run without one. Also rescoped both `tests/memory` scenarios asserting the ADR-0014 one-model-per-corpus guard to the fixture root — they asserted over the whole corpus, correct only while the database was empty; with real vault facts present the guard legitimately saw the configured provider's model id (same class of bug as `670f516`'s `test_claim_next_and_mark_failed`: passing on an empty database, failing against real data). MEASURED (commit message): 627 passed, 1 skipped, corpus unchanged across the run. |
| 2026-09-17 (measured by the orchestrator) | P8-T04 (pilot run) | orchestrator | With the wiring fix (`c462170`) in place, a deliberately scoped Tier 2 pilot ran against Bedrock Haiku 4.5: **15 of 144 queued episodes, 0 failed, 199.6s total, ~13.3 s/episode** (2–3 LLM calls/episode — the earlier 7.74s single-call P4-T02 median does not apply). Produced 129 entities, 147 facts, 81 artifacts, 145 mentions, 0 facts with incomplete provenance. Predicates: USES 35, RELATED_TO 31, PART_OF 22, USES_ARCHITECTURE 17, HAS_OWNER 14, DEPLOYED_ON 14. The pilot was **deliberately scoped** to 15 non-sensitive technical documents, explicitly excluding `06 Outputs/Career Docs`, `06 Outputs/Motivation Letters`, `00 Inbox/job-positions`, `02 Areas/Career Development`, `AIOS/me.md`, `skill-map`, and `INTERVIEW_COACH` — episode priorities were temporarily altered to control selection and restored afterwards; verified after the run by querying `facts.source_uri` that none of those excluded paths reached the model. **129 episodes remain `pending`** — this is a partial run, not the full background completion the task's acceptance criteria describe. |
| 2026-09-17 (measured by the orchestrator) | full suite | orchestrator | `docker compose --profile tools run --rm tools pytest -q` → **627 passed, 1 skipped**. The one skip is the Windows-junction test (`tests/unit/test_contracts_uri.py`, same platform skip as before). The former "no Tier-1 corpus" skip is gone because a real corpus now exists (P7-T02). This supersedes the 2026-09-15 428-passed figure in `reports/test-summary.md`, which has not yet been updated to reflect it before this reconciliation pass. |
| 2026-09-17 | — | A01 | Reports (`agent-runs.jsonl`, `task-ledger.md`, `build-timeline.md`, `test-summary.md`, `CHANGELOG.md`, `docs/development/task-plan.md`) reconciled against the nine commits above. P7-T01/T02 moved to done (P7-T02 at measured 98.6%, not rounded to 100%); P9-T01, P10-T01, P10-T02 moved to done; P8-T04 moved to **partial** (15/144 episodes, not done); P7-T03 left **todo** — Neo4j still holds 0 nodes and `aimemory-ingest rebuild-graph` fails on a missing driver function. Findings not tied to a single task (test-suite-destroys-corpus, chunker defects, invalid SQL doc, memory-api restart loop, Bedrock container auth, image build-context) are recorded above and in `reports/test-summary.md`/`CHANGELOG.md`. Nothing in this pass was committed by A01 per instruction — changes are left in the working tree for Mohammad to review and commit. |
