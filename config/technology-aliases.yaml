# Seed aliases for deterministic entity resolution (Technology + Project). Extend freely.
version: 0.1.0
technologies:
  PostgreSQL: [postgres, postgresql, pg]
  pgvector: [pg_vector, pgvector]
  Neo4j: [neo4j, neo4j browser]
  Ollama: [ollama]
  Qwen3: [qwen3, qwen 3, qwen3:4b]
  Databricks: [databricks, azure databricks, databricks free edition, adb]
  MLflow: [mlflow]
  Delta Lake: [delta, delta lake, delta tables]
  Unity Catalog: [unity catalog, uc]
  Snowflake: [snowflake]
  Tableau: [tableau, tableau public]
  Supabase: [supabase]
  Convex: [convex]
  SvelteKit: [sveltekit, svelte kit, svelte]
  Next.js: [nextjs, next.js, next]
  FastAPI: [fastapi]
  AWS Lambda: [lambda, aws lambda]
  AWS Fargate: [fargate, ecs fargate]
  Terraform: [terraform]
  Wagtail: [wagtail]
  Cloudflare: [cloudflare, cloudflare tunnel, cloudflare r2]
  Railway: [railway]
  Docker: [docker, docker compose, compose]
  PySpark: [pyspark, spark]
  R: [r language, r pipeline]
  Obsidian: [obsidian]
  Claude Code: [claude code]
  MCP: [mcp, model context protocol]
  Graphiti: [graphiti, graphiti-core]
  LangChain: [langchain]
projects:
  joblab-lakehouse-de: [JobLab Lakehouse, JobLab DE, JobLab Lakehouse (DE), DE]
  joblab-data-pipeline: [JobLab Data Pipeline, JobLabScraperAPI, joblab-data-pipeline]
  joblab-agent-backend: [JobLab Agent Backend, lambda_backend]
  joblab-analytics-frontend: [JobLab Analytics Frontend, job-analytics-frontend2]
  jobpilot: [JobPilot, Nova]
  oploy-website: [Oploy Website, oploy.eu, Wagtail site]
  content-center: [Content Center]
  senior-ai-eng: [Senior AI ENG, Senior AI Engineering, SAE]
  # order-to-plan: [Order-to-Plan, Order to Plan]
  #   Disabled 2026-09-17: no project with this id exists in the registry seeded from
  #   AIOS/me.md + AIOS/Maps/project-graph.md, and no registry id is an unambiguous match.
  #   entities.project_id is a foreign key to projects(id), so an alias pointing at a
  #   non-existent project makes every entity that matches it unwritable. Re-enable with the
  #   real id if this project is added to the registry.
  stateful-rem-on-databricks: [Stateful REM on Databricks, PHD-DS, Stateful HMM-REM]
  hmm-rem-r-pipeline-phd-vault: [HMM-REM R pipeline, HMM-REM, REM_HMM]
  azure-databricks-lab: [Azure Databricks lab, adb-dp750, DP-750]
  optimization-or-portfolio: [Optimization, OR portfolio, Operations Research portfolio]
  personal-harness: [Personal Harness, Harness]
  home-infrastructure: [Home Infrastructure, Huawei Ubuntu]
  account-center: [Account Center]
  career-evidence-apply: [Career Evidence, Apply, Career Evidence + Apply]
  interview-prep: [Interview prep]
